# Macro6 更新日志

## v1.62 — 2026-09-21

- 按下`+`后，从等待`CONNECT_OK`开始即持续并行检测Switch连接错误弹窗；等待`CONNECT_OK`、按A提交以及等待CODE画面三个阶段均不会再被错误黑框永久卡住。
- 按键1检测到该错误后沿用既有网络异常结算并从HOME重开；按键2同样从HOME重开且取得新CODE后停止；按键3/4直接从HOME执行网络异常重开。

## v1.61 — 2026-09-20

- Cloudflare实时状态改为事件驱动整包推送：本地仍每2秒检查一次，但只有CODE、阶段、玩家、任务、计数等公开语义状态变化时才立即上传。
- 移除云端实时包中的本地心跳时间、逐帧识别结果和逐秒倒计时；倒计时改传固定截止时间，由访问者浏览器每秒本地推进，静止画面不再持续消耗请求。
- 无状态变化时的Cloudflare保活由10秒降低为300秒；旧版配置会自动迁移，离线判定相应调整为420秒。
- 其它玩家开门列表取消每30秒轮询；提交、反馈、删除、管理员开关及数据导入发生变化时，通过既有WebSocket向所有访问者广播完整列表。
- 最新开门列表整包同时缓存在休眠WebSocket对象中，新访客建连即收到；仅部署后首次无缓存或WebSocket失败时回退查询D1。
- watchdog正常退出时，启动器会在关闭本机网页前同步提交最后一份`离线 / watchdog已停止`状态；异常强杀仍使用420秒心跳超时兜底。
- 车辆12小时过期和失效后30分钟隐藏由浏览器本地计时完成；WebSocket断线重连改为2秒至60秒指数退避，避免服务异常时产生请求风暴。

## v1.60 — 2026-09-18

- 04:50–05:00日期保护等待期间恢复原有300秒空闲保活：宏流程仍冻结，但连续300秒没有手柄输入时照常发送一次`DPAD上 → DPAD下`，避免画面变暗。

## v1.59 — 2026-09-18

- 新增北京时间04:50–05:00业务日保护窗：按键1开放流程首次在原始当前帧确认有效CURSOR后，立即暂停宏及全部手柄输入，等待05:00再执行一次日期切换重开。
- 保护等待使用无手柄输入的心跳等待，不触发300秒DPAD上下空闲保活；仍允许按0紧急中断，watchdog视频识别与网页状态继续运行。
- 保护窗只使用当前帧CURSOR，不使用750ms历史光标保留值，避免过渡画面的旧CURSOR误触发；已经处于任一重开流程时仍不会进入保护等待或追加日期重开。

## v1.58 — 2026-09-18

- 恢复北京时间05:00业务日切换自动重开：仅按键1开放流程中、当前未处于重开时触发一次完整重开。
- 正常重开、15分钟重开、30分钟严重超时重开或网络异常重开跨过05:00时，会在恢复STAMP循环前消费日期边界，不再重复注入日期重开。
- 日期触发的结束截图与玩家轮次汇总写入`date_change_reopen`，同时保存中文原因`日期切换重开`；终端及网页状态显示“日期切换重开”。

## v1.57 — 2026-09-17

- 修复网页“材料单车”提交被误判为“开门类型无效”：Cloudflare Worker此前把13字符的`material_solo`按12字符截断；现改为由允许类型集合自动计算最大长度并统一校验，避免枚举与长度限制再次不同步。

## v1.56 — 2026-09-16

- 按键1、2新增`BLACK SCREEN=ON`动态入口：不再重复HOME启动流程，直接等待黑屏消失，稳定3000ms后继续开门与CODE识别。
- 按键1、2新增四个存档画面动态入口：`GO_GAME`、`DOWNLOAD`、`CLOSE_GAME`、`OVERWRITE`均从当前阶段继续，仅执行当前及后续阶段，不重复此前存档操作。
- 新增`POK_SAV_STATE=true`动态入口：直接执行`HOME → A×4`并进入黑屏流程；上述入口取得新CODE后，按键1继续循环，按键2停在新CODE。

## v1.55 — 2026-09-16

- OVERWRITE阶段确认成功后新增500ms等待，再执行`DPAD上 → 等待500ms → A`，随后进入`POK_SAV_STATE=true`对勾等待。

## v1.54 — 2026-09-16

- 将`重开stage-无法覆盖数据.png`作为覆盖阶段的明确反例；OVERWRITE除灰度结构匹配外，还必须通过亮橙红色按钮占比校验。
- 灰暗的“无法覆盖数据”按钮不再触发`DPAD上 → A`，其它三个重开阶段的识别规则不变。

## v1.53 — 2026-09-16

- 按键1、2新增`CONNECT_OK`动态入口：直接按A续接重开的最后阶段并等待新CODE，不再从读档流程开头重来；按键1取得CODE后继续循环，按键2取得CODE后结束。

## v1.52 — 2026-09-16

- 完整读档重开在`DPAD下×3 → A → 等待500ms → A`后停止固定连按，改为四阶段视觉状态机。
- 依次确认“可前往对应游戏（无法备份）”“可下载数据”“关闭对应游戏”“覆盖数据”；每个阶段连续两帧确认且只接受预期的下一阶段，避免旧画面残留造成重复按键。
- 四张参考图的32×16灰度结构特征已压缩内嵌到watchdog6，运行时不读取外部图片。
- 覆盖数据阶段执行`DPAD上 → 等待500ms → A`后，才进入`POK_SAV_STATE=true`持续等待。

## v1.51 — 2026-09-13

- 顶部“玩家开门”按钮新增红色数字角标，显示云端当前有效、未满且未失效的开放车辆数量，并每30秒刷新。

## v1.50 — 2026-09-13

- 按指定文字重写福利侧栏的“好友宝可梦动作”说明，并接入好友宝可梦位置与动作对照两张图片。
- 任务指南改用烟花燃放点、图图犬住处与材料、材料箱与展示框、做饭位置、亮闪闪位置等实际配图。

## v1.49 — 2026-09-13

- 小红书开放播报末尾提示由“可访问 rabi.date 查看”改为“大家也可在页面上传自己的开门码”。
- 修复侧栏内容隔离：排行榜的“总计/日/周/月/年/精准查询”只在排行榜中显示，不再覆盖福利、玩家开门和好人榜。
- 好人榜按“总计/日/周/月/年/精准查询”查询网站访客发布的开车记录，并在玩家总次数下细分各车种次数。
- 新增“花车”和“材料单车”；只有梦幻章车提供“梦幻章满/已满有误”反馈。
- 玩家发布的开门码改为必填且严格校验6位数字或英文字母。

## v1.48 — 2026-09-13

- 将独立“数据查询”入口合并为“排行榜”的“精准查询”子页面；排行榜顶部子页面顺序调整为“总计、日、周、月、年、精准查询”。
- 常规排行榜与精准查询结果的榜单顺序统一调整为“任务榜、来访榜、失败榜”。

## v1.47 — 2026-09-13

- 公网页面新增“福利”“玩家开门”“好人榜”三个互斥侧栏；福利页提供动作路线和七类任务的固定图文指南。
- 玩家开门信息常驻Cloudflare D1：支持梦幻章/任务/其它类型、描述、已满/失效反馈、发布者本地凭证删除；信息12小时过期，确认失效30分钟后隐藏，自行删除不计入好人榜。
- 好人榜只统计网站访客提交且未自行删除的开门记录，支持北京时间05:00业务日口径的日、周、月、年和总计排行；Windows离线时Cloudflare仍可独立提供提交、查询与反馈。
- 社区写操作采用每地址限频、每日充足额度、活跃车辆上限和新车失效双地址确认；后台仅保存加盐IP哈希及上传、反馈、早期失效反馈次数，不保存明文IP。
- 新增管理员总开关与`toggle_pokopia_community_writes.bat`；关闭后禁止公开提交与反馈但保留发布者纠错删除。新增鉴权备份下载/合并上传接口及`pokopia_community_admin.py`命令行入口。

## v1.46 — 2026-09-13

- 房间冻结按原因拆分：超时、15分钟或STAMP:NO触发的正常重开继续归为“房间关闭时未抵达”；检测到连接错误并直接从HOME开始的异常重开改归为“网络连接错误时未抵达”。
- 异常重开的每轮玩家总结新增`network_connection_error.players_not_arrived`名单；每名未抵达玩家也写入`network_error_before_arrival`，不再被普通重开冻结分类抽走。
- 网页“近期轮次回顾”会直接列出网络连接错误时尚未抵达的玩家名单。
- 网络异常未抵达仍计入本轮、当日和总进入失败人数，但新增独立的按玩家日/总永久计数；本地CMD、网页查询和Cloudflare排行榜均展示第三类失败原因。
- Cloudflare D1新增网络异常失败字段迁移；历史旧记录由于当时未保存可区分的重开原因，不会被猜测性重分类，新分类从可判定的日志与本版本起累计。

## v1.45 — 2026-09-13

- 调整完整读档重开序列中`DPAD下×3`之后的等待：连续六次A依次等待500ms、5000ms、500ms、5000ms、2000ms、5000ms；随后仍执行DPAD上、A并持续等待绿色下载完成对勾。
- 仅修改上述等待时长，不改变按键数量、顺序、POK_SAV_STATE判定或后续开门流程。

## v1.44 — 2026-09-12

- 新轮次CODE面板连续10次无法得到六码时，不再等待3分钟后重开：第10次失败帧只归档一张error截图，本轮CODE标记为“未知”，随后继续正常开放流程。
- 未知CODE播报显示“当前轮次：[未知]”；公网网页显示醒目的`？？？？？？`、禁用复制按钮，并把同一张失败帧同步到CODE截图区，供玩家自行读取密语。
- CODE识别成功或触发未知回退时，CODE字段与“开放中”状态一次性写入网页状态；开放播报也在此时生成，不再等到返回CURSOR或进入STAMP循环。
- 按键1在播报后继续原有等待5秒、B→上→右→下与STAMP循环；按键2仅在未知CODE分支执行同一返回动作并停在CURSOR:STAMP，有效六码仍保持停在CODE页面。
- 未知回退后冻结当前CODE面板的后续OCR，直到该面板实际消失才允许下一轮识别，避免迟到的OCR结果覆盖已经公开的未知轮次。

## v1.43 — 2026-09-12

- 按键1循环刷章和按键2单次刷章共用的小红书开放播报末尾新增独立一行：`可访问 rabi.date 查看`。
- 网页公开的完整播报文本与Chrome实际发送内容继续共用同一份字符串，因此同步展示该访问提示；不改变CODE识别、轮次统计、消息发送或宏流程。

## v1.42 — 2026-09-12

- 修复Cloudflare Static Assets优先直接返回根域首页、导致 `rabi.date` 未进入Worker的308跳转逻辑；随后页面API跨域跳转被浏览器拦截，表现为公网页面加载成功但实时数据不更新。
- 将根路径 `/` 加入选择性 `assets.run_worker_first`，现在只让首页、API、媒体和健康检查进入Worker；CSS、JavaScript和图案等静态资源继续由Cloudflare边缘直接缓存，不额外消耗Worker请求。
- 公网核验确认 `stamp.rabi.date/health` 正常，且 `stamp.rabi.date/api/live` 已收到Windows上传的实时CODE、轮次、玩家与计时数据；边缘上传链路本身无需修改。

## v1.41 — 2026-09-12

- 修复Windows部署器直接启动 `npm.cmd` / `npx.cmd` 时，部分Python/Windows组合会在Cloudflare登录前异常退出的问题；现统一通过 `cmd.exe` 执行Node命令。
- 部署BAT本身改为纯ASCII文本并移除CMD括号内的中文提示，避免中文Windows按GBK/UTF-8错误解码后把乱码提示误当成命令执行。
- Windows Node命令进一步改为由 `node.exe` 直接执行 `npm-cli.js` / `npx-cli.js`，彻底绕开 `cmd.exe /S /C` 对 `C:\Program Files` 带空格路径的错误去引号行为；非标准Node目录才使用 `call` 兼容回退。
- 修复Wrangler 4的R2列表命令不支持 `--json`、在部分Windows Node版本下因此以 `0xC0000409` 异常终止的问题；R2改为解析原生命令输出，D1仍使用其正式支持的JSON输出。
- 部署器在捕获型Cloudflare命令失败时会先打印完整命令输出再终止，不再只显示进程退出码。
- Cloudflare返回R2未启用错误10042时，部署器现在会直接提示Dashboard中的启用路径，并明确无需创建S3 API Token或手工存储桶。
- Cloudflare返回首次使用Workers所需的10063初始化错误时，部署器会提示打开账户级 `Workers & Pages` 并设置一次workers.dev账户子域；该内部子域不会被配置为本站公开入口。
- `deploy_pokopia_cloudflare.bat` 改为打开独立且不会自动关闭的部署窗口，启动即显示标题和进度；Node或Python缺失、部署成功或失败都会保留明确提示。
- Python部署异常会额外写入根目录 `pokopia_cloudflare_deploy_error.log`，便于在浏览器登录尚未出现时定位具体失败步骤。
- Wrangler身份检查同时校验退出代码与“未认证”输出；只有确实未登录时才调用系统默认浏览器完成Cloudflare授权，不固定指定Chrome。

## v1.40 — 2026-09-11

- 正式加入 `rabi.date → stamp.rabi.date` 的Cloudflare边缘发布方案：Windows只进行出站HTTPS上传，不开放路由器端口、不把家庭公网IP写入DNS。
- 实时状态存入Durable Object并通过WebSocket主动推送到页面；公开页倒计时每秒本地推进，WebSocket断开时才进行低频降级读取，避免按访客数量产生每秒轮询。
- 最新CODE截图固定覆盖R2单一对象，历史日/周/月/年/总计和玩家模糊查询数据存入D1；原始日志、ERROR图、姓名图、结束图、F10图、Chrome 9222及串口继续保持私有。
- 查询条件仅存在各自浏览器会话中，历史查询与排行榜分接口限流；每个 `IP + 浏览器会话 + 接口` 每秒最多一次，不同访问者互不影响。
- Windows一键启动器在检测到DPAPI加密配置后自动启动独立边缘上传进程；断网和上传失败只退避重试，不会停止或干扰Macro6。
- 新增 `deploy_pokopia_cloudflare.bat`：引导登录Cloudflare，创建/复用D1及R2、部署Worker和两个Custom Domain，并自动配置秘密上传令牌。

## v1.39 — 2026-09-11

- CODE卡片右下角新增用户提供的梦幻章图案，以低透明度背景形式展示，不遮挡六位CODE及状态文字。
- 网页媒体入口改为只公开 `/media/current-code.png`：每次新CODE截图到来后使用同一路径替换并刷新；历史CODE图、结束图、姓名图、手动截图及ERROR图均不能通过网页媒体路径枚举访问。
- 新增图片保留维护：网页服务后台每天清理一次超过30天的非ERROR PNG/JPG/WEBP；ERROR诊断图和全部JSON/JSONL/TXT日志永久保留。
- 图片保留天数可在 `pokopia_web_config.json` 通过 `image_retention_days` 调整，默认30天；维护不改变Macro6识别、归档或重开逻辑。

## v1.38 — 2026-09-11

- 删除严重超时播报与日志中的人工联系提示；严重超时原因保留为“严重超时，自动重开”，相关重开判断和宏动作不变。

## v1.37 — 2026-09-11

- 公网部署暂缓，当前交付固定为Windows本机运行；不修改路由器、端口映射、Cloudflare或域名。
- 网页与watchdog一键入口改用Windows专用总启动器：确认网页真正就绪后才打开默认浏览器并调用原Macro6 BAT，避免固定等待2秒造成慢机器启动竞态。
- 总启动器增加UTF-8输出、缺文件、8787端口占用及启动超时提示；正常退出、Ctrl+C和直接关闭启动窗口时，通过Windows Job Object回收网页及watchdog子进程，避免后台残留占用端口或设备。
- 网页单独启动BAT同步启用Python UTF-8及无缓冲输出；网页前端不需要Node.js、Java或额外Web框架，继续使用现有Python环境。
- 页面15分钟倒计时改为直接跟随watchdog发布的实际剩余秒数，并在两次页面轮询之间本地递减，不再依赖浏览器自行解析预计结束时间。

## v1.36 — 2026-09-11

- 新增独立只读实时网页服务：大号CODE及对应截图、细分轮次状态、Chrome同源播报摘要、任务3格进度、15分钟倒计时、房间玩家、本轮任务及玩家进出时间线、近期轮次回顾。
- 新增日/周/月/年/总计前10名侧栏与任意业务日期范围、玩家名模糊匹配查询；输出来访、任务参与、两类失败排行及逐日服务列表，周仍以周五05:00为边界。
- Macro6仅通过限频原子JSON发布状态，不开放写入API，不改变按键、识别、超时和重开判断；20秒无快照自动显示离线。
- 公网媒体路由只允许CODE成功截图；ERROR、F10、结束截图、姓名裁剪和原始日志不对外开放。
- 新增网页单独启动BAT、网页与watchdog一键启动BAT、Cloudflare Tunnel及动态公网IP端口映射部署说明；域名注册、Cloudflare账户、路由器和防火墙继续等待用户确认。

## v1.35 — 2026-09-06

- 修复按键1已经进入完整读档/重开流程后，30分钟严重超时仍能穿透 `disarm()`，再次中断并从摇杆上下左右开头重启的问题。
- 从返回宏开头、进入等待存档对勾所属的完整重开流程起，15分钟和30分钟超时均不再注入；取得新CODE后的5秒等待、B→上→右→下及CURSOR定位仍保持关闭，真正进入持续STAMP循环检测前才重新启用两级超时。
- STAMP:NO、15分钟、30分钟、CODE识别超时及网络异常触发下一次重开时都会再次关闭两级超时；暂不新增心跳重启后的断点恢复逻辑。

## v1.34 — 2026-09-05

- 周、月、年统计新增与“全部日期”一致的逐业务日期列表，逐日展示成功服务人次和进入失败人次。
- 每个周期的逐日列表末尾新增“总计”行；总计只汇总所选周、月或年的业务日期范围，原有玩家来访榜、任务榜和失败榜保持不变。

## v1.33 — 2026-09-05

- 修复网络异常重开在 Windows 文件版本不同步时调用旧版 `_run_smart_macro_6_part_1`，因其不接受 `start_at_home` 参数而导致 watchdog 异常退出的问题。
- 重开/读档第一段现由 Macro6 watchdog 本文件直接实现，不再依赖 `smart_macro_gamepad.py` 中同名函数的参数版本；只替换 watchdog 文件也能使用网络异常从 HOME 开始的入口。
- 普通重开仍执行摇杆上下左右、B×6、`+`、`-`、方向上、A 后进入 HOME；网络异常重开仍跳过这些动作并直接从 HOME 开始，其余流程不变。

## v1.32 — 2026-09-05

- 新增统一入口 `查询日周月年统计.cmd`，启动后通过1～4选择日、周、月、年统计；首层直接回车默认日统计。
- 统一入口继续复用各周期既有默认值：日留空=上一个业务日；周年月留空=当前业务周；周编号留空=所选月份第1周；月留空=当前业务月；年留空=当前业务年。
- 原有独立日、周、月、年CMD继续保留兼容，统计数据及时间边界不变。

## v1.31 — 2026-09-05

- 新增周、月、年三个统计CMD，全部输出范围内CODE轮次、成功/失败人次、玩家来访榜、任务榜和合并原因的失败榜。
- 周统计固定为周五05:00至下周五04:59:59；输入 `YYYYMM` 后列出该月内所有周五起始的4～5周并二次选择，周编号留空选择第1周，年月留空直接查询当前业务周。
- 月统计使用每月1日05:00至下月1日04:59:59；输入 `YYYYMM`，留空使用当前业务月。
- 年统计使用每年1月1日05:00至次年1月1日04:59:59；输入 `YYYY`，留空使用当前业务年。
- 周、月、年的玩家榜单都在查询时将范围内逐日数据重新汇总并按次数降序排列，不新增固定周表、月表或年表。

## v1.30 — 2026-09-05

- 指定日期查询直接回车时，默认查询北京时间05:00分界下的上一个业务日期；显式输入 `YYYYMMDD` 的行为不变。
- 全部日期统计菜单直接回车时，默认选择2，仅展示业务日期、服务人次和失败人次。
- 两个查询提示均直接标明空输入的默认行为。

## v1.29 — 2026-09-05

- 玩家失败查询合并为每名玩家一行：`玩家A，进入失败N次（到达前返回 X次，房间关闭时未抵达 Y次）`，不再拆成三张重复名单。
- `输出全部日期服务人数.cmd` 新增启动菜单：1全部展示；2仅业务日期/服务人次/失败人次；3玩家来访；4玩家任务；5玩家失败。
- 选项3～5展示对应历史总榜；所有榜单保持查询时动态按次数降序排列，指定日期查询继续输出所选业务日的全部明细。

## v1.28 — 2026-09-05

- 永久统计新增按玩家名划分的进入失败次数，并拆分为“到达前返回”和“房间关闭时仍未抵达”两种客观原因；每日及历史总计分别保存。
- 新生成的每轮玩家访问记录增加 `failure_reason` 和 `failure_reason_text`，失败原因无需再仅通过 `ended_by_room_freeze` 推断。
- 指定日期和全部日期统计接口增加失败总榜及两类原因分榜，所有榜单仍只在输出时动态按次数降序排列。
- 任务参与窗口调整为“即将抵达→回去了”：路上期间先暂存任务，只有该次访问后来确认“已抵达”才正式计入该玩家；到达前返回或房间关闭时仍未抵达均不计任务。
- 玩家确认抵达时会一次性补计加载期间暂存的任务，抵达后的REWARD继续按实际新增条数累计，同一访问仍最多3项。
- v1.28首次启动会从既有每轮总结恢复两类实名失败；一次性历史补全脚本还会重建未总结事件，并按 `available_reward_tasks` 恢复历史成功访问的任务参与数，以符合新的完整访问窗口口径。

## v1.27 — 2026-09-05

- 新增一次性历史衔接脚本 `一次性补全历史玩家统计.cmd`，动态定位项目，不写死电脑路径。
- 同时扫描既有 `PLAYERS_日期.jsonl` 每轮总结和 `STAMP_日期.jsonl` 玩家状态/任务进度，补全尚未总结的成功来访与参与完成任务次数。
- 现有永久计数、每轮总结统计、详细事件重建统计按业务日及玩家名取最大可靠值，重复执行不会把同一批历史记录反复相加。
- 写入前要求确认watchdog已经关闭，自动备份原 `macro6_watchdog_stats.json`，以临时文件原子替换，并在日志目录生成独立的 `PLAYER_STATS_BACKFILL_时间.json` 补全报告。

## v1.26 — 2026-09-05

- `CURSOR:STAMP` 的独立梦幻章检查周期由10秒缩短为7秒。
- 周期到达时仍必须即时确认画面为 `CURSOR:STAMP` 才按A；其他CURSOR或INVALID均不按A，并顺延至后续首次重新确认STAMP的时刻。
- 按A后等待2秒再恢复持续检测STAMP结果的规则保持不变。

## v1.25 — 2026-09-05

- 永久统计新增按玩家名划分的来访次数：玩家首次进入“已抵达”状态时，本业务日和历史总计分别加1；同一玩家多次进入按多次来访统计。
- 永久统计新增按玩家名划分的参与完成任务数：REWARD确认增加任务时，对当时已经抵达且仍在房间中的每位玩家按实际新增任务条数累计；路上、已离开及进入失败的玩家不计入。
- 每名玩家每次来访的任务数仍受原有单轮 `3/3` 上限保护，同一REWARD页面不会重复累计。
- 指定日期查询增加“当日玩家来访次数”和“当日玩家参与完成任务数”两张列表；全部日期查询增加对应的历史总计列表。
- 永久文件只维护可动态增加玩家的计数字典，不维护固定名次；每次查询时才按次数从高到低排序，次数相同时按玩家名排序。
- 首次加载v1.25时会扫描既有 `PLAYERS_日期.jsonl` 的每轮汇总，一次性补全可恢复的历史玩家来访和参与任务次数；之后直接增量维护，不会在每次查询时重新扫描或重复累计。

## v1.24 — 2026-09-04

- 按键1、2因 `NETWORK_ERROR` 结束本轮时，重开不再执行摇杆上下左右、B×6、`+`、`-`、方向上和A；直接从第一个 `HOME` 开始执行读档及后续开门流程。
- 盖满、15分钟超时、30分钟严重超时、CODE OCR超时及普通手动重开仍使用原完整重开序列，不受网络异常专用入口影响。
- 每次完整重开均在首个控制动作之前同步保存一张采集卡原始画面；普通重开在摇杆上下左右之前保存，网络异常重开在HOME之前保存。
- 本轮结束截图沿用 `SCREENSHOT_UTC时间` 命名，并追加 `_<当前CODE>_结束.png`；CODE为空时写作 `_EMPTY_结束.png`。
- 同一条业务日JSONL记录截图对应的本日轮次、总轮次、CODE、宏按键及重开原因，终端也同步输出这些信息。

## v1.23 — 2026-09-04

- 新增独立 `repair_player_name_right_edge(image)` 接口，输入已经切好的玩家姓名BGR图像，输出右边缘修复图像。
- 修复只作用于姓名最后一个橙色像素列右侧的尾部padding：其中黑色、灰色或极暗像素替换为纯白；橙色笔画以及最后橙色列左侧的所有像素逐字节保持不变。
- 玩家通知调用顺序改为“原始帧截图 → 姓名切块 → 右边缘修复 → PaddleOCR识别 → 返回姓名处理”。
- `Name` 文件夹现在保存实际送入OCR的右边缘修复后截图；识别成功和“未知”均使用同一修复图，不再保存修复前的姓名裁剪。

## v1.22 — 2026-09-03

- 修复 Windows 上玩家姓名接口初始化时的 PaddleOCR/PyTorch DLL 导入顺序冲突：不再预先 `import paddle`，由 PaddleOCR 在 PaddleX/ModelScope 完成模型源初始化后自行加载推理后端，避免 `torch\\lib\\shm.dll` 报 `WinError 127`。
- 单核限制继续由 `OMP_NUM_THREADS=1`、`MKL_NUM_THREADS=1` 和 PaddleOCR `cpu_threads=1` 保证，不依赖提前导入 PaddlePaddle。
- 独立命令现在会先检查输入图片是否存在；路径错误时直接显示解析后的绝对路径，不再先初始化大型模型。

## v1.21 — 2026-09-03

- 玩家姓名识别拆分为独立 `player_name_recognizer.py` 接口；输入姓名裁剪图片或 NumPy 图像，只输出识别文字，不包含玩家状态、房间状态、宽度判断、姓名词典或人工纠正规则。
- watchdog 玩家名生产路径改用 PaddleOCR 官方 `PP-OCRv6_medium_rec` 模型，停止调用内嵌姓名字形层、RapidOCR候选组合、Windows OCR和单字符人工保底。
- PP-OCRv6单模型覆盖中文、英文、日文及46种拉丁语系；模型在后台预热，推理限制为单线程，避免阻塞视频显示和桌面操作。
- 官方模型未返回姓名或不可用时，沿用v1.20规则：保存一张“未知”姓名裁剪、消费该横幅段且不更新房间人员。
- 修正 Windows Python 3.11+ 的 NumPy 固定版本为 `2.3.5`，同时满足 OpenCV 的 `numpy>=2` 与 PaddleX 的 `numpy<2.4`，恢复 `pip install -r requirements.txt` 一次性解析安装。

## v1.20 — 2026-09-03

- 玩家横幅已经通过联机球、状态文字和橙色姓名区域检查，但所有姓名OCR路径仍返回空值时，将该帧的原始姓名裁剪保存到 `Name` 文件夹并以“未知”命名。
- “未知”截图仍同步写入 `姓名识别记录.txt`，保留横幅状态和原始采集时间，便于后续补充字库或核验OCR问题。
- 保存一张“未知”截图后立即消费并清理该视觉横幅段，不再反复OCR、重复输出错误或阻塞后续玩家通知；未知玩家不会写入房间人员状态与服务人数统计。

## v1.19 — 2026-09-02

- 撤销 v1.10～v1.12 将字符数量置于所有证据之前的姓名候选策略；普通姓名恢复为早期版本的独立OCR来源共识优先，不再把 `佳佳` 宽度内的 `土土土土` 当成“更完整”。
- 姓名候选新增橙色墨迹宽高模型：全角中日韩字符、拉丁宽字/窄字、数字和符号使用不同的预估字宽；候选总宽与原始橙色姓名宽度明显不符时直接退出竞争。
- `八彩彩` 与 `彩彩` 现在通过三字符/两字符的实际宽度是否吻合判别，不再通过“谁更长谁获胜”判别。
- 恢复早期稳定的原始抗锯齿彩色裁剪作为首个OCR输入；橙色隔离原图、柔化图和二值图保留为辅助，右侧黑色状态笔画仍不能通过橙色宽度校验。
- 单字符 `L` 继续使用完全独立的几何分支，不参与普通中文、日文或多字符姓名的候选排序。
- 新增压缩的单字符全角字形层；按橙色宽度切分字符槽并逐字符匹配，内嵌的是可重新组合的字符模板而非完整玩家姓名。字形误差或第一、第二候选间距不达标时自动回退通用OCR，不强行套用已有字符。

## v1.18 — 2026-09-02

- 按键1、2、4共用的开门尾段取消按 `+` 后固定等待20秒；改为持续等待 `CONNECT_OK=ON`，随后按 `A` 50ms并等待500ms进入CODE等待状态。
- 按键1、2、4从上述 `A` 的实际发送时间开始等待CODE画面；30秒仍没有CODE画面且画面仍为 `CONNECT_OK=ON` 时再次按 `A`，之后按同一30秒规则继续保护。
- 30秒补按只允许在即时确认 `CONNECT_OK=ON` 时执行，CODE画面及其他状态不会误按。

## v1.17 — 2026-09-02

- 玩家姓名 OCR 输入改为仅保留与强橙色笔画相连的橙色抗锯齿像素，其余像素全部置白。
- 右端残留的黑色状态笔画、蓝/黄色界面横线不再进入通用 OCR，避免在玩家名后虚构 `l`、`[`、`」` 等字符。
- 橙色原始变体、橙色灰度变体和二值变体现在使用同一橙色支持区，不对识别结果做姓名字典替换。
- 同长候选改为以 OCR 置信度为主、来源共识为小幅加分；避免两个相关的二值变体以低置信度压过明显更清晰的橙色原图结果。

## v1.16 — 2026-08-31

- `Name` 文件夹新增 UTF-8 文本索引 `姓名识别记录.txt`；每张姓名原始裁剪保存成功后同步追加一行。
- 每行记录 UTC 原始采集时间、对应截图文件名、OCR 原样识别名以及通知状态，便于后期逐图核对。
- 文本中的识别名不进行文件名安全替换，可保留 OCR 当时输出的精确原文。

## v1.15 — 2026-08-31

- 玩家通知通过现有跨帧确认并实际改变房间状态后，单独保存该帧中的玩家姓名原始裁剪。
- 新样本只包含橙色玩家姓名对应的原始画面小区域，不保存整帧、watchdog 标注画面或 OCR 二值化变体。
- 截图统一存放在归档根目录的 `Name` 文件夹，文件名为 UTC 原始采集时间加 OCR 识别的玩家名；仅对 Windows 不允许的文件名字符做安全替换，不修改识别结果。
- 姓名符号清洗功能仍保持暂停，本版未新增任何姓名清洗规则。

## v1.14 — 2026-08-31

- 新增 Switch 连接错误弹窗检测，以 `SCREENSHOT_20260831T103601_694441Z` 左下方固定“关闭”按钮文字为锚点；特征以压缩掩码内嵌在 watchdog 中，运行时不读取 `Pic` 图片。
- 连续两帧命中才输出 `NETWORK ERROR: TRUE`，watchdog 同时用独立方框标出“关闭”锚点。
- 按键1开放轮次检测到新弹窗后，立即结束本轮、保留本轮开放时长和任务数，并回到完整读档/重开入口；正常序列的第一个 HOME 继续负责冻结并结算房间玩家。
- 按键2的动态开放阶段同样处理该异常；完成完整重开并取得新 CODE 后仍按按键2原规则停止。
- 弹窗异常在重开期间被抑制，残留弹窗不会再次注入重开；下一轮重新出现时才会产生新的上升沿事件。
- 下一次 CODE 播报的上轮原因新增“上轮开放时长X分Y秒，发生网络波动断开，重新开启新的一轮”，并继续在同一行附加任务完成数和服务百变怪数。
- 指定反例 `capture_20260821_184326_577861_00001`、`POKOPIA_01_BLACK_0`、`POKOPIA_02_BLACK_1` 均不得命中异常。

## v1.13 — 2026-08-31

- Chrome 小红书输入接口新增可选 `send=False`；省略该参数时仍保持原有输入并按 Enter 发送的行为。
- `chrome_code_input.py` 命令行新增 `--no-send`，用于只把测试内容填入当前聊天框而不发送。
- 命令行新增 `--text`，可以直接指定包含对应六位 CODE 的完整测试文字。
- Macro6 的现有播报调用不传 `send` 参数，因此仍会正常自动发送。

## v1.12 — 2026-08-28

- 修复多个 OCR 来源共同给出较短名字时压过单个完整候选的问题，例如已有 `rapid/orange` 识别到 `八彩彩`，最终却采用 `彩彩`。
- 三套 RapidOCR 图像变体现在全部执行，不再因前两个较短结果达成共识而提前跳过最后一个变体。
- 玩家名候选改为字符长度最高优先；存在 `八彩彩`、`彩彩`、`八` 时，只让最长的 `八彩彩` 进入来源共识、可信度比较和跨帧投票。
- `incomplete` 不再只代表“多字画面识别成单字”；只要本轮存在字符数更多的候选，所有较短候选都标记为不完整。
- 长度比较基于 Unicode 可见字符数量，不使用固定字符像素宽度；组合附加符、变体选择符和连接符不被错误计为独立字符。

## v1.11 — 2026-08-27

- 保留 v1.10 的完整玩家名优先规则；只要任一 OCR 路径取得完整候选，就不会使用残缺候选。
- 当全部 OCR 路径都被判为 `incomplete` 时，不再完全丢弃本次玩家通知；按字符数最长、独立 OCR 来源共识最多、可信度最高的顺序选择兜底名字。
- `incomplete` 兜底结果仍须通过原有的跨帧两票确认后才能更新房间，不会根据单帧残缺识别立即增删玩家。
- 终端 OCR 候选日志新增 `incomplete-fallback=...`，明确标注本次房间更新使用了残缺兜底。

## v1.10 — 2026-08-27

- 修复完整双字/多字玩家名被通用 OCR 高置信度截成首字后提前通过的问题，例如 `来须` 被记录为 `来`。
- 完整姓名裁剪和可变字符宽度保持不变；新增规则仅以原始橙色墨迹整体宽高判断画面是否明显存在多个字形，不按固定单字宽度切割。
- 当画面明显为多字形时，单字符 OCR 结果标记为 `incomplete`，不能参与候选胜出或触发提前结束；继续尝试二值变体及 Windows 中/英文 OCR。
- 真正单字名与 v1.5 的大写 `L` 几何保底保持有效；无法取得完整候选时宁可继续重试，不再把明显残缺的首字写入房间状态。
- 终端会保留被拒绝短候选的 OCR 来源和 `incomplete` 标记，便于后续核验。

## v1.9 — 2026-08-26

- 玩家横幅在主视频线程生成原始裁剪时立即分配单调递增的 `capture_sequence`、采集时间和房间代次。
- 原始顺序元数据贯穿后台分析、视觉分段、OCR队列、两帧投票及房间状态应用；不再以OCR完成时间代表事件顺序。
- 增加硬顺序门：准备应用的事件若不晚于最后已应用的原图序号，直接丢弃，不允许旧OCR结果在稍后插入。
- 每个玩家记录最后事件序号和离开标记；离开后迟到的“已抵达”不会重新加入，必须先收到原图序号更新的“即将抵达”才能开始新访问。
- 新CODE清空玩家生命周期序号和离开标记；新一轮首次直接出现“已抵达”仍可正常接收。
- 玩家状态JSON新增 `ocr_player_name`、`capture_sequence`、`capture_time`、`segment_id` 和 `processing_delay_ms`，便于核验原图顺序与OCR延迟。
- 回归中修正 `capture_20260824_170628_353544_00003` 的既有状态误判：淡出灰色尾迹使宽度变长时，明显更优的“已抵达”固定模板可以覆盖宽度规则。

## v1.8 — 2026-08-26

- 修复离开横幅把 `百變怪` 截短识别为 `百`、把 `哈哈` 截短识别为 `哈` 后无法移除原玩家的问题。
- `left` 事件优先在当前房间玩家中进行唯一包含关系匹配；单字截断仅在唯一候选时归并，避免多个相同首字玩家之间误删。
- 非包含型 OCR 小误差可在相似度不低于 0.60 且领先第二候选至少 0.15 时归并；原有 0.78 通用阈值保持不变。
- 自动纠正时终端输出原始 OCR 名与最终房间名；JSON 玩家状态记录使用纠正后的完整名字。
- 单字符 `L` 几何保底规则未扩展到中文字符，本次问题不是 v1.5 主动单字识别造成的。

## v1.7 — 2026-08-26

- 将五分钟手柄保活设为 F12 操作锁的唯一后台输入例外。
- F12 锁定期间仍按实际手柄活动计算 300 秒；到期发送一次 DPAD 上、下，防止等待 `POK_SAV_STATE` 时画面变暗。
- F12 继续禁止全部人工键盘操作，不会放行宏触发、中断、退出、截图或普通手动控制键。

## v1.6 — 2026-08-26

- 按 Nintendo Switch 官方“减少屏幕灼伤”五分钟无操作变暗时间，新增 300 秒全局手柄输入空闲计时器。
- 宏动作和所有有效的正常手动控制按键都会重置空闲计时；F10、统计键及无效键不冒充手柄活动。
- 连续 300 秒没有任何实际手柄按下时发送 `DPAD上50ms → 归中100ms → DPAD下50ms → 归中`，随后重新计时。
- 等待 `POK_SAV_STATE` 小对号、视觉状态等待、宏内长等待、P 暂停和 Macro6 待机状态均受同一保活计时器保护。
- 此版本最初在 F12 操作锁期间禁止保活；该规则已由 v1.7 调整为允许保活。

## v1.5 — 2026-08-26

- 修复单字符玩家 `L 已抵达。` 被漏识别的问题。
- 玩家状态掩码只保留与橙色名字同一文字行，避免横幅底边及背景灰色像素把“已抵达。”错误扩宽为“即将抵达。”。
- 为形状证据充分的单个大写 `L` 增加橙色字形几何保底；命中后直接输出 `L`，无需等待通用 OCR。
- 使用原始 ERROR 截图回放验证，结果为 `L / arrived`；回收箱过滤仍将该画面正确保留为玩家通知。

## v1.4 — 2026-08-25

- 将联机球后的固定“回收箱中存有物品。”系统横幅与玩家进出横幅分开识别。
- 回收箱横幅命中后不进入玩家名 OCR、不累计右侧横幅识别失败，也不再生成玩家通知 ERROR 截图或 error 日志。
- 回收箱文字/图标特征以内嵌压缩掩码保存在 watchdog 代码中，不依赖外部参考图片。
- 回收箱通知会结束当前视觉横幅分段，但不会增删玩家、改变房间状态或写入玩家事件。

## v1.3 — 2026-08-25

- RapidOCR/ONNX Runtime 固定为单核顺序推理，不再按默认设置占用全部物理核心。
- 玩家 OCR 工作线程在 Windows 中降为低于正常优先级，采集、控制、鼠标及前台软件优先获得 CPU 时间。
- Windows OCR 回退使用低优先级 PowerShell 子进程。
- 原始图和橙色柔化图已经识别出相同名字时，跳过第三套二值图推理，减少常规玩家名约三分之一的 ONNX 计算。
- OCR 精度投票和 FIFO 顺序保持不变；疑难名字在低资源模式下可能需要更长时间完成。

## v1.2 — 2026-08-25

- 玩家通知识别改为两级后台流水线，视频显示线程不再执行横幅文字分析或 OCR 预处理。
- 主线程只进行小区域联机球粗检测，并按 50ms 周期复制原始横幅裁剪。
- 后台分析线程负责橙色名字分割、固定状态识别、视觉指纹和横幅分段。
- 独立 OCR 线程负责生成原始、柔化、二值化三套玩家名输入并执行识别。
- OCR 较慢时，后续横幅仍由后台分析线程持续分段并按 FIFO 顺序等待，不阻塞 Watchdog 画面刷新。
- 玩家通知识别失败的完整原始画面截图改为后台请求、由下一次主循环按需复制，正常帧不再额外复制整幅画面。

## v1.1 — 2026-08-25

- 玩家横幅按视觉变化分段并进入异步 FIFO，避免较慢的玩家名 OCR 阻塞后续通知。
- 联机球持续显示时仍可依据名字及状态横幅变化区分不同通知；每个横幅段最多保留八帧。
- 暂时停用北京时间 05:00 自动重开；05:00 业务日计数、文件夹及日志归档规则保持不变。
- 玩家正常进出状态和玩家任务进度重新写入整体 `STAMP_日期.jsonl`。
- `PLAYERS_日期.jsonl` 只保留每轮 `room_reopen_summary` 汇总。
- 玩家日志删除 `recorded_at_utc`、`stamp_operational_date` 和 `day_boundary_beijing`。
- `recorded_at_beijing` 更名为 `record_time`，时间内容不再附带 `+08:00`。
- 玩家访问记录字段简化为 `incoming`、`arrived` 和 `ended`，删除重复的 `entry_time`。
